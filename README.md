# SH-APK-API

This repository is intended for an APK-related API service.

## Project status

- Local project directory: `c:\Users\Notandi\code\sh-apk-api`
- Remote GitHub repository: `https://github.com/Skragus/SH-APK-API.git`

The GitHub repo is currently empty. Once `git` is installed and configured on this machine, you can connect and push like this (from the project directory):

```bash
git init
git remote add origin https://github.com/Skragus/SH-APK-API.git
git add .
git commit -m "Initial commit"
git push -u origin main
```

If you prefer working on a `dev` branch first:

```bash
git checkout -b dev
git push -u origin dev
```

